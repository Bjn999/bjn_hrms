{
  "مقبول": "",
  "جيد": "",
  "جيد مرتفع": "",
  "جيد جداً": "",
  "إمتياز": "",
  "صورة شخصية (اختياري)": "",
  "السيرة الذاتية (اختياري)": "",
  "أرشيف الراتب": "",
  "تحديث الصورة الشخصية (اختياري)": "",
  "اختر صورة جديدة لاستبدال الصورة الحالية": "",
  "تحديث السيرة الذاتية (اختياري)": "",
  "يوجد ملف سيرة ذاتية في النظام": "",
  "أرشيف رواتب الموظف": "",
  "تاريخ ووقت الأرشفة": "",
  "الراتب": "",
  "الإضافة": "",
  "لا يوجد رواتب مؤرشفة لهذا الموظف": "",
  "setShowSalaryArchiveModal(false)} className=\"w-full bg-slate-100 text-slate-900 border-2 border-slate-200 py-4 rounded-2xl font-black hover:bg-slate-200 transition-all active:scale-95\">إغلاق": "",
  "صورة الموظف الحالية": "",
  "{ fetchData(); }, []); const fetchData = async () => { try { const token = localStorage.getItem('admin_token'); const res = await fetch(`http://localhost:8000/api/admin/employees`, { headers: { 'Authorization': `Bearer ${token}`, 'Accept': 'application/json' } }); const result = await res.json(); if (result.status) { setData(result.data); } } catch (e) { showToast(t('fetch_failed'), 'error'); } finally { setLoading(false); } }; const handleDelete = async (id: number) => { const isConfirmed = await confirm({ title: t('confirm_delete'), description: 'هل انت متأكد من حذف هذا الموظف؟', icon: 'danger' }); if (!isConfirmed) return; try { const token = localStorage.getItem('admin_token'); const res = await fetch(`http://localhost:8000/api/admin/employees/${id}`, { method: 'DELETE', headers: { 'Authorization': `Bearer ${token}`, 'Accept': 'application/json' } }); const result = await res.json(); if (result.status) { fetchData(); showToast(result.message, 'success'); } else { showToast(result.message, 'error'); } } catch (e) { showToast(t('conn_error'), 'error'); } }; if (loading && data.length === 0) return": "",
  "setSearchQuery(e.target.value)} placeholder=\"بحث شامل (برقم الموظف، الاسم، الإدارة، الفرع، الفئة الوظيفية، الحالة...)\" className=\"w-full pr-12 pl-4 py-3 bg-slate-50 border border-slate-200 rounded-2xl focus:ring-2 focus:ring-indigo-500/20 outline-none text-slate-700 font-bold transition-all placeholder:text-slate-400 placeholder:font-normal\" />": "",
  "بحث شامل (برقم الموظف، الاسم، الإدارة، الفرع، الفئة الوظيفية، الحالة...)": "",
  "({ name: '', file_path: null }); const [isFileSubmitting, setIsFileSubmitting] = useState(false); useEffect(() => { fetchData(); }, []); const fetchData = async () => { try { const token = localStorage.getItem('admin_token'); // Fetch employee details const res = await fetch(`http://localhost:8000/api/admin/employees/${id}`, { headers: { 'Authorization': `Bearer ${token}`, 'Accept': 'application/json' } }); const result = await res.json(); if (result.status) { setData(result.data); } else { showToast(result.message, 'error'); router.push('/admin/employees'); } // Fetch active allowances for the modal const settingsRes = await fetch('http://localhost:8000/api/admin/employees/required-data', { headers: { 'Authorization': `Bearer ${token}`, 'Accept': 'application/json' } }); const settingsResult = await settingsRes.json(); if (settingsResult.status) { setAllowancesList(settingsResult.data.allowances || []); } } catch (e) { showToast(t('fetch_failed'), 'error'); } finally { setLoading(false); } }; const handleOpenModal = (mode: 'add' | 'edit', item: any = null) => { setModalMode(mode); if (mode === 'edit' && item) { setEditingAllowanceId(item.id); setModalAllowanceId(String(item.allowance_id)); setModalValue(String(item.value)); } else { setEditingAllowanceId(null); setModalAllowanceId(''); setModalValue(''); } setShowModal(true); }; const handleSubmitAllowance = async (e: any) => { e.preventDefault(); if (!modalAllowanceId || !modalValue) { showToast(t('select_option'), 'error'); return; } if (modalMode === 'add') { const isDuplicate = data?.fixed_allowances?.some((item: any) => String(item.allowance_id) === String(modalAllowanceId)); if (isDuplicate) { showToast(language === 'ar' ? 'عفواً هذا البدل مسجل لهذا الموظف من قبل' : 'Allowance already exists for this employee', 'error'); return; } } setIsSubmitting(true); try { const token = localStorage.getItem('admin_token'); const url = modalMode === 'add' ? `http://localhost:8000/api/admin/employees/${id}/fixed-allowances` : `http://localhost:8000/api/admin/employees/fixed-allowances/${editingAllowanceId}`; const res = await fetch(url, { method: modalMode === 'add' ? 'POST' : 'PUT', headers: { 'Authorization': `Bearer ${token}`, 'Content-Type': 'application/json', 'Accept': 'application/json' }, body: JSON.stringify({ allowance_id: parseInt(modalAllowanceId), value: parseFloat(modalValue) }) }); const result = await res.json(); if (result.status) { showToast(modalMode === 'add' ? t('allowance_added_success') : 'تم التعديل بنجاح', 'success'); setShowModal(false); fetchData(); } else { showToast(result.message, 'error'); } } catch (err) { showToast(t('update_error'), 'error'); } finally { setIsSubmitting(false); } }; const handleDeleteAllowance = async (allowanceId: number) => { const isConfirmed = await confirm({ title: t('confirm_delete'), description: 'هل أنت متأكد من رغبتك في حذف هذا البدل؟', icon: 'danger' }); if (!isConfirmed) return; try { const token = localStorage.getItem('admin_token'); const res = await fetch(`http://localhost:8000/api/admin/employees/fixed-allowances/${allowanceId}`, { method: 'DELETE', headers: { 'Authorization': `Bearer ${token}`, 'Accept': 'application/json' } }); const result = await res.json(); if (result.status) { showToast(t('allowance_deleted_success'), 'success'); fetchData(); } else { showToast(result.message, 'error'); } } catch (err) { showToast(t('update_error'), 'error'); } }; const fetchSalaryArchive = async () => { try { const token = localStorage.getItem('admin_token'); const res = await fetch(`http://localhost:8000/api/admin/employees/${id}/salary-archive`, { headers: { 'Authorization': `Bearer ${token}`, 'Accept': 'application/json' } }); const result = await res.json(); if (result.status) { setSalaryArchiveData(result.data); setShowSalaryArchiveModal(true); } else { showToast(result.message || t('fetch_failed'), 'error'); } } catch (error) { showToast(t('conn_error'), 'error'); } }; const handleAddFile = async (e: React.FormEvent) => { e.preventDefault(); if (!fileFormData.name || !fileFormData.file_path) { showToast('يجب إدخال اسم الملف واختيار الملف', 'warning'); return; } setIsFileSubmitting(true); try { const token = localStorage.getItem('admin_token'); const payload = new FormData(); payload.append('name', fileFormData.name); payload.append('file_path', fileFormData.file_path); const res = await fetch(`http://localhost:8000/api/admin/employees/${id}/files`, { method: 'POST', headers: { 'Authorization': `Bearer ${token}`, 'Accept': 'application/json' }, body: payload }); const result = await res.json(); if (result.status) { showToast(result.message, 'success'); setShowFileModal(false); setFileFormData({ name: '', file_path: null }); fetchData(); } else { showToast(result.message, 'error'); } } catch (err) { showToast('حدث خطأ في الاتصال', 'error'); } finally { setIsFileSubmitting(false); } }; const handleDeleteFile = async (fileId: number) => { const isConfirmed = await confirm({ title: 'تأكيد الحذف', description: 'هل أنت متأكد من رغبتك في حذف هذا الملف؟', icon: 'danger' }); if (!isConfirmed) return; try { const token = localStorage.getItem('admin_token'); const res = await fetch(`http://localhost:8000/api/admin/employees/files/${fileId}`, { method: 'DELETE', headers: { 'Authorization': `Bearer ${token}`, 'Accept': 'application/json' } }); const result = await res.json(); if (result.status) { showToast(result.message, 'success'); fetchData(); } else { showToast(result.message, 'error'); } } catch (err) { showToast('حدث خطأ في الاتصال', 'error'); } }; const handleDownloadFile = async (fileName: string) => { try { const url = `http://localhost:8000/assets/admin/uploads/${fileName}`; const response = await fetch(url); const blob = await response.blob(); const downloadUrl = window.URL.createObjectURL(blob); const link = document.createElement('a'); link.href = downloadUrl; link.download = fileName; document.body.appendChild(link); link.click(); document.body.removeChild(link); window.URL.revokeObjectURL(downloadUrl); } catch(e) { window.open(`http://localhost:8000/assets/admin/uploads/${fileName}`, '_blank'); } }; if (loading || !data) return": "",
  "{data.graduation_estimate == 1 ? 'مقبول' : data.graduation_estimate == 2 ? 'جيد' : data.graduation_estimate == 3 ? 'جيد مرتفع' : data.graduation_estimate == 4 ? 'جيد جداً' : data.graduation_estimate == 5 ? 'إمتياز' : '---'}": "",
  "{data.emp_military_id == 1 ? 'أدى الخدمة' : data.emp_military_id == 2 ? 'إعفاء' : data.emp_military_id == 3 ? 'تأجيل' : '---'}": "",
  "السيرة الذاتية للموظف": "",
  "{data.emp_cv ? 'ملف مرفق' : 'لا يوجد سيرة ذاتية'}": "",
  "تحميل": "",
  "الملفات المرفقة للموظف": "",
  "رفع ملف جديد": "",
  "اسم الملف": "",
  "تاريخ الرفع": "",
  "الإجراءات": "",
  "handleDownloadFile(file.file_path)} className=\"w-8 h-8 rounded-lg bg-indigo-50 text-indigo-600 flex items-center justify-center hover:bg-indigo-100 transition-all\" title=\"تحميل / عرض\" >": "",
  "handleDeleteFile(file.id)} className=\"w-8 h-8 rounded-lg bg-rose-50 text-rose-600 flex items-center justify-center hover:bg-rose-100 transition-all\" title=\"حذف\" >": "",
  "لا توجد ملفات مرفقة لهذا الموظف": "",
  "{modalMode === 'add' ? t('add_fixed_allowance') : 'تعديل البدل الثابت'}": "",
  "اسم الملف (وصف للملف)": "",
  "setFileFormData({...fileFormData, name: e.target.value})} placeholder=\"مثال: صورة الجواز، صورة الهوية...\" className=\"w-full bg-slate-50 border border-slate-200 rounded-xl px-4 py-3 text-slate-800 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-transparent font-bold transition-all\" />": "",
  "اختر الملف": "",
  "الصيغ المدعومة: الصور ومستندات PDF والوورد": "",
  "{isFileSubmitting ? 'جاري الرفع...' : 'رفع وحفظ'}": "",
  "setShowFileModal(false)} className=\"flex-1 bg-slate-100 text-slate-700 py-3 rounded-xl hover:bg-slate-200 transition-all font-black\" > إلغاء": "",
  "تحميل / عرض": "",
  "حذف": "",
  "مثال: صورة الجواز، صورة الهوية...": "",
  "يجب إدخال اسم الملف واختيار الملف": "",
  "حدث خطأ في الاتصال": "",
  ") => { setFormData({ ...formData, [e.target.name]: e.target.value }); }; const handleSubmit = async (e: React.FormEvent) => { e.preventDefault(); try { const token = localStorage.getItem('admin_token'); const url = modalType === 'add' ? 'http://localhost:8000/api/admin/branches' : `http://localhost:8000/api/admin/branches/${editingId}`; const method = modalType === 'add' ? 'POST' : 'PUT'; const res = await fetch(url, { method: method, headers: { 'Content-Type': 'application/json', 'Authorization': `Bearer ${token}`, 'Accept': 'application/json' }, body: JSON.stringify(formData) }); const result = await res.json(); if (result.status) { setShowModal(false); fetchData(); showToast(result.message, 'success'); } else { showToast(result.message, 'error'); } } catch (e) { showToast(t('conn_error'), 'error'); } }; const handleDelete = async (id: number) => { const isConfirmed = await confirm({ title: t('confirm_delete'), description: 'هل أنت متأكد من رغبتك في حذف هذا الفرع؟', icon: 'danger' }); if (!isConfirmed) return; try { const token = localStorage.getItem('admin_token'); const res = await fetch(`http://localhost:8000/api/admin/branches/${id}`, { method: 'DELETE', headers: { 'Authorization': `Bearer ${token}`, 'Accept': 'application/json' } }); const result = await res.json(); if (result.status) { fetchData(); showToast(result.message, 'success'); } else { showToast(result.message, 'error'); } } catch (e) { showToast(t('conn_error'), 'error'); } }; if (loading) return": "",
  "لم يتم التحديث": "",
  "لا يمكن تعطيله لأنه مستخدم في بيانات أحد الموظفين": "",
  ") => { setFormData({ ...formData, [e.target.name]: e.target.value }); }; const handleSubmit = async (e: React.FormEvent) => { e.preventDefault(); try { const token = localStorage.getItem('admin_token'); const url = modalType === 'add' ? 'http://localhost:8000/api/admin/departments' : `http://localhost:8000/api/admin/departments/${editingId}`; const method = modalType === 'add' ? 'POST' : 'PUT'; const res = await fetch(url, { method: method, headers: { 'Content-Type': 'application/json', 'Authorization': `Bearer ${token}`, 'Accept': 'application/json' }, body: JSON.stringify(formData) }); const result = await res.json(); if (result.status) { setShowModal(false); fetchData(); showToast(result.message, 'success'); } else { showToast(result.message, 'error'); } } catch (e) { showToast(t('conn_error'), 'error'); } }; const handleDelete = async (id: number) => { const isConfirmed = await confirm({ title: t('confirm_delete'), description: 'هل أنت متأكد من رغبتك في حذف هذه الإدارة؟', icon: 'danger' }); if (!isConfirmed) return; try { const token = localStorage.getItem('admin_token'); const res = await fetch(`http://localhost:8000/api/admin/departments/${id}`, { method: 'DELETE', headers: { 'Authorization': `Bearer ${token}`, 'Accept': 'application/json' } }); const result = await res.json(); if (result.status) { fetchData(); showToast(result.message, 'success'); } else { showToast(result.message, 'error'); } } catch (e) { showToast(t('conn_error'), 'error'); } }; if (loading) return": "",
  "الاضافة": "",
  "التحديث": "",
  "= { 1: 'يناير', 2: 'فبراير', 3: 'مارس', 4: 'أبريل', 5: 'مايو', 6: 'يونيو', 7: 'يوليو', 8: 'أغسطس', 9: 'سبتمبر', 10: 'أكتوبر', 11: 'نوفمبر', 12: 'ديسمبر' }; return months[item.month_id] || item.month_id; }; const getRowAction = (item: any) => { if (item.is_open == 2) return 'archived'; if (item.is_open == 1) return 'view'; return 'pending'; // Absences shouldn't be \"opened\" from here, just viewed or pending. }; if (loading) return": "",
  "اسم الشهر عربي": "",
  "تاريخ البداية": "",
  "تاريخ النهاية": "",
  "بداية البصمة": "",
  "نهاية البصمة": "",
  "عدد الأيام": "",
  "حالة الشهر": "",
  "مفتوح": "",
  "عرض": "",
  "مغلق": "",
  "بانتظار الفتح": "",
  "{settings?.company_name || 'نظام إدارة الموارد البشرية'}": "",
  "كشف غيابات الموظفين": "",
  "تاريخ الطباعة: {new Date().toLocaleDateString('ar-EG')}": "",
  "وقت الطباعة: {new Date().toLocaleTimeString('ar-EG')}": "",
  "للشهر المالي: {financeMonth.month?.name} لسنة {financeMonth.finance_yr}": "",
  "م": "",
  "اسم الموظف": "",
  "أيام الغياب": "",
  "قيمة الخصم": "",
  "ملاحظات": "",
  "{item.value} يوم": "",
  "لا توجد بيانات لهذا الشهر": "",
  "توقيع المحاسب": "",
  "توقيع مدير الموارد البشرية": "",
  "الاعتماد": "",
  "(null); const [form, setForm] = useState({ ...emptyForm }); const headers = () => ({ 'Authorization': `Bearer ${localStorage.getItem('admin_token')}`, 'Accept': 'application/json', 'Content-Type': 'application/json', }); const fetchData = async () => { try { const res = await fetch(`${API}/absences/${monthId}`, { headers: headers() }); const result = await res.json(); if (result.status) { setFinanceMonth(result.finance_month); setAbsences(result.absences || []); setEmployees(result.employees || []); const fm = result.finance_month; setIsMonthOpen(fm?.is_open == 1); } else { showToast(result.message, 'error'); router.push('/admin/absences'); } } catch { showToast(t('conn_error'), 'error'); } finally { setLoading(false); } }; useEffect(() => { fetchData(); }, []); const openAddModal = () => { setEditingId(null); setForm({ ...emptyForm, finance_month_period_id: monthId }); setShowModal(true); }; const openEditModal = (a: any) => { if (a.is_archived == 1) { showToast(t('cannot_edit_archived'), 'error'); return; } if (!isMonthOpen) { showToast(t('cannot_edit_closed'), 'error'); return; } setEditingId(a.id); setForm({ finance_month_period_id: monthId, employee_code: a.employee_code, day_price: a.day_price, value: a.value, notes: a.notes || '', }); setShowModal(true); }; const handleEmployeeChange = (code: string) => { const emp = employees.find((e: any) => e.employee?.employee_code == code || e.employee_code == code); const dayPrice = emp?.employee?.day_price || emp?.day_price || ''; setForm(f => ({ ...f, employee_code: code, day_price: dayPrice })); }; const handleSave = async () => { if (!form.employee_code || !form.value || !form.day_price) { showToast('يرجى تعبئة جميع الحقول المطلوبة', 'error'); return; } if (!editingId) { const exists = absences.some((a: any) => String(a.employee_code) === String(form.employee_code)); if (exists) { const ok = await confirm({ title: 'تنبيه', description: 'هذا الموظف لديه سجل غياب بالفعل في هذا الشهر. هل تريد الإضافة على أي حال؟', icon: 'warning' }); if (!ok) return; } } setSaving(true); try { const url = editingId ? `${API}/absences/${editingId}` : `${API}/absences`; const method = editingId ? 'PUT' : 'POST'; const res = await fetch(url, { method, headers: headers(), body: JSON.stringify(form) }); const result = await res.json(); if (result.status) { showToast(editingId ? t('absence_updated') : t('absence_added'), 'success'); setShowModal(false); fetchData(); } else { showToast(result.message, 'error'); } } catch { showToast(t('conn_error'), 'error'); } finally { setSaving(false); } }; const handleDelete = async (a: any) => { if (a.is_archived == 1) { showToast(t('cannot_delete_archived'), 'error'); return; } if (!isMonthOpen) { showToast(t('cannot_delete_closed'), 'error'); return; } const ok = await confirm({ title: t('confirm_delete'), description: 'هل أنت متأكد من حذف سجل الغياب؟', icon: 'danger' }); if (!ok) return; try { const res = await fetch(`${API}/absences/${a.id}`, { method: 'DELETE', headers: headers() }); const result = await res.json(); if (result.status) { showToast(t('absence_deleted'), 'success'); fetchData(); } else showToast(result.message, 'error'); } catch { showToast(t('conn_error'), 'error'); } }; const filteredAbsences = absences.filter((a: any) => { if (!searchQuery) return true; const query = searchQuery.toLowerCase(); const empName = a.employee?.emp_name?.toLowerCase() || ''; const empCode = a.employee_code?.toString() || ''; const notes = a.notes?.toLowerCase() || ''; const value = a.value?.toString() || ''; const total = a.total?.toString() || ''; return empName.includes(query) || empCode.includes(query) || notes.includes(query) || value.includes(query) || total.includes(query); }); const totalDays = filteredAbsences.reduce((s: number, a: any) => s + parseFloat(a.value || 0), 0); const totalAmount = filteredAbsences.reduce((s: number, a: any) => s + parseFloat(a.total || 0), 0); if (loading) return": "",
  "طباعة الكشف": "",
  "setSearchQuery(e.target.value)} placeholder=\"بحث شامل (برقم الموظف، الاسم، عدد الأيام، الملاحظات، المبلغ...)\" className=\"w-full pr-12 pl-4 py-3 bg-slate-50 border border-slate-200 rounded-2xl focus:ring-2 focus:ring-emerald-500/20 outline-none text-slate-700 font-bold transition-all placeholder:text-slate-400 placeholder:font-normal\" />": "",
  "عرض الملاحظة": "",
  "لا يوجد تحديث": "",
  "بحث شامل (برقم الموظف، الاسم، عدد الأيام، الملاحظات، المبلغ...)": "",
  "يرجى تعبئة جميع الحقول المطلوبة": "",
  "= { 1: 'يناير', 2: 'فبراير', 3: 'مارس', 4: 'أبريل', 5: 'مايو', 6: 'يونيو', 7: 'يوليو', 8: 'أغسطس', 9: 'سبتمبر', 10: 'أكتوبر', 11: 'نوفمبر', 12: 'ديسمبر' }; return months[item.month_id] || item.month_id; }; if (loading) return": "",
  "إضافي الأيام": "",
  "إدارة السجلات الخاصة بإضافي أيام الموظفين مقسمة حسب الأشهر المالية": "",
  "لا يوجد سجلات": "",
  "كشف إضافات أيام للموظفين": "",
  "الأيام المضافة": "",
  "قيمة الإضافة": "",
  "(null); const [form, setForm] = useState({ ...emptyForm }); const headers = () => ({ 'Authorization': `Bearer ${localStorage.getItem('admin_token')}`, 'Accept': 'application/json', 'Content-Type': 'application/json', }); const fetchData = async () => { try { const res = await fetch(`${API}/additions/${monthId}`, { headers: headers() }); const result = await res.json(); if (result.status) { setAdditions(result.data || []); setEmployees(result.employees || []); setFinanceMonth(result.finance_month); setIsMonthOpen(result.finance_month?.is_open == 1); } else { showToast(result.message, 'error'); } } catch { showToast(t('conn_error'), 'error'); } finally { setLoading(false); } }; useEffect(() => { fetchData(); }, []); const openAddModal = () => { setEditingId(null); setForm({ ...emptyForm, finance_month_period_id: monthId }); setShowModal(true); }; const openEditModal = (a: any) => { if (a.is_archived == 1) { showToast(t('cannot_edit_archived'), 'error'); return; } if (!isMonthOpen) { showToast(t('cannot_edit_closed'), 'error'); return; } setEditingId(a.id); setForm({ finance_month_period_id: monthId, employee_code: a.employee_code, day_price: a.day_price, value: a.value, notes: a.notes || '', }); setShowModal(true); }; const handleEmployeeChange = (code: string) => { const emp = employees.find((e: any) => e.employee_code == code); const dayPrice = emp?.day_price || ''; setForm(f => ({ ...f, employee_code: code, day_price: dayPrice })); }; const handleSave = async () => { if (!form.employee_code || !form.value || !form.day_price) { showToast('يرجى تعبئة جميع الحقول المطلوبة', 'error'); return; } if (!editingId) { const exists = additions.some((a: any) => String(a.employee_code) === String(form.employee_code)); if (exists) { const ok = await confirm({ title: 'تنبيه', description: 'هذا الموظف لديه إضافي أيام بالفعل في هذا الشهر. هل تريد الإضافة على أي حال؟', icon: 'warning' }); if (!ok) return; } } setSaving(true); try { const url = editingId ? `${API}/additions/${editingId}` : `${API}/additions`; const method = editingId ? 'PUT' : 'POST'; const res = await fetch(url, { method, headers: headers(), body: JSON.stringify(form), }); const result = await res.json(); if (result.status) { showToast('تم حفظ الإضافي بنجاح', 'success'); setShowModal(false); fetchData(); } else { showToast(result.message, 'error'); } } catch { showToast(t('conn_error'), 'error'); } finally { setSaving(false); } }; const handleDelete = async (a: any) => { if (a.is_archived == 1) { showToast(t('cannot_delete_archived'), 'error'); return; } if (!isMonthOpen) { showToast(t('cannot_delete_closed'), 'error'); return; } const ok = await confirm({ title: t('confirm_delete'), description: 'هل أنت متأكد من حذف الإضافي؟', icon: 'danger' }); if (!ok) return; try { const res = await fetch(`${API}/additions/${a.id}`, { method: 'DELETE', headers: headers() }); const result = await res.json(); if (result.status) { showToast('تم حذف الإضافي بنجاح', 'success'); fetchData(); } else showToast(result.message, 'error'); } catch { showToast(t('conn_error'), 'error'); } }; const filteredAdditions = additions.filter((a: any) => { if (!searchQuery) return true; const query = searchQuery.toLowerCase(); const empName = a.employee?.emp_name?.toLowerCase() || ''; const empCode = a.employee_code?.toString() || ''; const notes = a.notes?.toLowerCase() || ''; const value = a.value?.toString() || ''; const total = a.total?.toString() || ''; return empName.includes(query) || empCode.includes(query) || notes.includes(query) || value.includes(query) || total.includes(query); }); const total = filteredAdditions.reduce((sum: number, a: any) => sum + parseFloat(a.total || 0), 0); const days = filteredAdditions.reduce((sum: number, a: any) => sum + parseFloat(a.value || 0), 0); if (loading) return": "",
  "إضافة إضافات أيام": "",
  "عدد الحركات": "",
  "إجمالي الأيام": "",
  "إجمالي القيمة": "",
  "setSearchQuery(e.target.value)} placeholder=\"بحث شامل (برقم الموظف، الاسم، عدد الأيام، الملاحظات...)\" className=\"w-full pr-12 pl-4 py-3 bg-slate-50 border border-slate-200 rounded-2xl focus:ring-2 focus:ring-emerald-500/20 outline-none text-slate-700 font-bold transition-all placeholder:text-slate-400 placeholder:font-normal\" />": "",
  "الإجمالي": "",
  "{a.value} أيام": "",
  "لا يوجد إضافي أيام": "",
  "{editingId ? 'تعديل إضافي الأيام' : 'إضافة أيام إضافية'}": "",
  "عدد الأيام *": "",
  "الإجمالي:": "",
  "بحث شامل (برقم الموظف، الاسم، عدد الأيام، الملاحظات...)": "",
  "تم حفظ الإضافي بنجاح": "",
  "تم حذف الإضافي بنجاح": "",
  "البدلات المتغيرة": "",
  "إدارة السجلات الخاصة بالبدلات المتغيرة مقسمة حسب الأشهر المالية": "",
  "كشف البدلات المتغيرة للموظفين": "",
  "نوع البدل": "",
  "القيمة": "",
  "(null); const [form, setForm] = useState({ ...emptyForm }); const headers = () => ({ 'Authorization': `Bearer ${localStorage.getItem('admin_token')}`, 'Accept': 'application/json', 'Content-Type': 'application/json', }); const fetchData = async () => { try { const res = await fetch(`${API}/employee-allowances/${monthId}`, { headers: headers() }); const result = await res.json(); if (result.status) { setAllowances(result.data || []); setEmployees(result.employees || []); setTypes(result.types || []); setFinanceMonth(result.finance_month); setIsMonthOpen(result.finance_month?.is_open == 1); } else { showToast(result.message, 'error'); } } catch { showToast(t('conn_error'), 'error'); } finally { setLoading(false); } }; useEffect(() => { fetchData(); }, []); const openAddModal = () => { setEditingId(null); setForm({ ...emptyForm, finance_month_period_id: monthId }); setShowModal(true); }; const openEditModal = (a: any) => { if (a.is_archived == 1) { showToast(t('cannot_edit_archived'), 'error'); return; } if (!isMonthOpen) { showToast(t('cannot_edit_closed'), 'error'); return; } setEditingId(a.id); setForm({ finance_month_period_id: monthId, employee_code: a.employee_code, allowances_id: a.allowances_id, total: a.total, notes: a.notes || '', }); setShowModal(true); }; const handleEmployeeChange = (code: string) => { setForm(f => ({ ...f, employee_code: code })); }; const handleSave = async () => { if (!form.employee_code || !form.allowances_id || !form.total) { showToast('يرجى تعبئة جميع الحقول المطلوبة', 'error'); return; } if (!editingId) { const exists = allowances.some((a: any) => String(a.employee_code) === String(form.employee_code) && String(a.allowances_id) === String(form.allowances_id)); if (exists) { const ok = await confirm({ title: 'تنبيه', description: 'هذا الموظف لديه هذا البدل في نفس الشهر. هل تريد الإضافة على أي حال؟', icon: 'warning' }); if (!ok) return; } } setSaving(true); try { const url = editingId ? `${API}/employee-allowances/${editingId}` : `${API}/employee-allowances`; const method = editingId ? 'PUT' : 'POST'; const res = await fetch(url, { method, headers: headers(), body: JSON.stringify(form), }); const result = await res.json(); if (result.status) { showToast('تم حفظ البدل بنجاح', 'success'); setShowModal(false); fetchData(); } else { showToast(result.message, 'error'); } } catch { showToast(t('conn_error'), 'error'); } finally { setSaving(false); } }; const handleDelete = async (a: any) => { if (a.is_archived == 1) { showToast(t('cannot_delete_archived'), 'error'); return; } if (!isMonthOpen) { showToast(t('cannot_delete_closed'), 'error'); return; } const ok = await confirm({ title: t('confirm_delete'), description: 'هل أنت متأكد من حذف البدل؟', icon: 'danger' }); if (!ok) return; try { const res = await fetch(`${API}/employee-allowances/${a.id}`, { method: 'DELETE', headers: headers() }); const result = await res.json(); if (result.status) { showToast('تم حذف البدل بنجاح', 'success'); fetchData(); } else showToast(result.message, 'error'); } catch { showToast(t('conn_error'), 'error'); } }; const filteredAllowances = allowances.filter((a: any) => { if (!searchQuery) return true; const query = searchQuery.toLowerCase(); const empName = a.employee?.emp_name?.toLowerCase() || ''; const empCode = a.employee_code?.toString() || ''; const notes = a.notes?.toLowerCase() || ''; const total = a.total?.toString() || ''; const typeObj = types.find((t: any) => t.id === a.allowances_id); const typeName = typeObj?.name?.toLowerCase() || ''; return empName.includes(query) || empCode.includes(query) || notes.includes(query) || total.includes(query) || typeName.includes(query); }); const total = filteredAllowances.reduce((sum: number, a: any) => sum + parseFloat(a.total || 0), 0); if (loading) return": "",
  "إضافة بدل متغير": "",
  "عدد البدلات": "",
  "setSearchQuery(e.target.value)} placeholder=\"بحث شامل (برقم الموظف، الاسم، نوع البدل، الملاحظات، المبلغ...)\" className=\"w-full pr-12 pl-4 py-3 bg-slate-50 border border-slate-200 rounded-2xl focus:ring-2 focus:ring-emerald-500/20 outline-none text-slate-700 font-bold transition-all placeholder:text-slate-400 placeholder:font-normal\" />": "",
  "{filteredAllowances.map((a: any) => { const isArchived = a.is_archived == 1; const canAct = isMonthOpen && !isArchived; const emp = employees.find((e: any) => String(e.employee_code) === String(a.employee_code)); const empName = a.employee?.emp_name || emp?.emp_name || a.employee_code; const typeName = types.find((t: any) => t.id == a.allowances_id)?.name || 'غير محدد'; return (": "",
  "لا توجد بدلات متغيرة مسجلة": "",
  "{editingId ? 'تعديل البدل' : 'إضافة بدل جديد'}": "",
  "نوع البدل *": "",
  "اختر نوع البدل": "",
  "المبلغ الإجمالي *": "",
  "بحث شامل (برقم الموظف، الاسم، نوع البدل، الملاحظات، المبلغ...)": "",
  "تم حفظ البدل بنجاح": "",
  "تم حذف البدل بنجاح": "",
  "الخصومات المالية": "",
  "إدارة وعرض الخصومات المالية للموظفين حسب الأشهر المالية": "",
  "السنة المالية": "",
  "الكل": "",
  "اسم الشهر المالي": "",
  "عرض الخصومات": "",
  "مؤرشف": "",
  "لا توجد بيانات متاحة": "",
  "كشف خصومات الموظفين": "",
  "(null); const [form, setForm] = useState({ ...emptyForm }); const headers = () => ({ 'Authorization': `Bearer ${localStorage.getItem('admin_token')}`, 'Accept': 'application/json', 'Content-Type': 'application/json', }); const fetchData = async () => { try { const res = await fetch(`${API}/discounts/${monthId}`, { headers: headers() }); const result = await res.json(); if (result.status) { setFinanceMonth(result.finance_month); setDiscounts(result.discounts || []); setEmployees(result.employees || []); setDiscountTypes(result.discount_types || []); const fm = result.finance_month; setIsMonthOpen(fm?.is_open == 1); } else { showToast(result.message, 'error'); router.push('/admin/discounts'); } } catch { showToast(t('conn_error'), 'error'); } finally { setLoading(false); } }; useEffect(() => { fetchData(); }, []); const openAddModal = () => { setEditingId(null); setForm({ ...emptyForm, finance_month_period_id: monthId }); setShowModal(true); }; const openEditModal = (a: any) => { if (a.is_archived == 1) { showToast(t('cannot_edit_archived'), 'error'); return; } if (!isMonthOpen) { showToast(t('cannot_edit_closed'), 'error'); return; } setEditingId(a.id); setForm({ finance_month_period_id: monthId, employee_code: a.employee_code, discounts_type_id: a.discounts_type_id, total: a.total, notes: a.notes || '', }); setShowModal(true); }; const handleSave = async () => { if (!form.employee_code || !form.discounts_type_id || !form.total) { showToast('يرجى تعبئة جميع الحقول المطلوبة', 'error'); return; } if (!editingId) { const exists = discounts.some((a: any) => String(a.employee_code) === String(form.employee_code)); if (exists) { const ok = await confirm({ title: 'تنبيه', description: 'هذا الموظف لديه خصم بالفعل في هذا الشهر. هل تريد الإضافة على أي حال؟', icon: 'warning' }); if (!ok) return; } } setSaving(true); try { const url = editingId ? `${API}/discounts/${editingId}` : `${API}/discounts`; const method = editingId ? 'PUT' : 'POST'; const res = await fetch(url, { method, headers: headers(), body: JSON.stringify(form) }); const result = await res.json(); if (result.status) { showToast(editingId ? 'تم تعديل الخصم بنجاح' : 'تم إضافة الخصم بنجاح', 'success'); setShowModal(false); fetchData(); } else { showToast(result.message, 'error'); } } catch { showToast(t('conn_error'), 'error'); } finally { setSaving(false); } }; const handleDelete = async (a: any) => { if (a.is_archived == 1) { showToast(t('cannot_delete_archived'), 'error'); return; } if (!isMonthOpen) { showToast(t('cannot_delete_closed'), 'error'); return; } const ok = await confirm({ title: t('confirm_delete'), description: 'هل أنت متأكد من حذف الخصم المالي؟', icon: 'danger' }); if (!ok) return; try { const res = await fetch(`${API}/discounts/${a.id}`, { method: 'DELETE', headers: headers() }); const result = await res.json(); if (result.status) { showToast('تم حذف الخصم بنجاح', 'success'); fetchData(); } else showToast(result.message, 'error'); } catch { showToast(t('conn_error'), 'error'); } }; const filteredDiscounts = discounts.filter((d: any) => { if (!searchQuery) return true; const query = searchQuery.toLowerCase(); const empName = d.employee?.emp_name?.toLowerCase() || ''; const empCode = d.employee_code?.toString() || ''; const notes = d.notes?.toLowerCase() || ''; const total = d.total?.toString() || ''; const typeLabel = d.discount_type?.name?.toLowerCase() || ''; return empName.includes(query) || empCode.includes(query) || notes.includes(query) || total.includes(query) || typeLabel.includes(query); }); const totalAmount = filteredDiscounts.reduce((s: number, a: any) => s + parseFloat(a.total || 0), 0); if (loading) return": "",
  "إضافة خصم": "",
  "إجمالي الحركات": "",
  "إجمالي المبالغ": "",
  "setSearchQuery(e.target.value)} placeholder=\"بحث شامل (برقم الموظف، الاسم، الملاحظات، المبلغ...)\" className=\"w-full pr-12 pl-4 py-3 bg-slate-50 border border-slate-200 rounded-2xl focus:ring-2 focus:ring-emerald-500/20 outline-none text-slate-700 font-bold transition-all placeholder:text-slate-400 placeholder:font-normal\" />": "",
  "نوع الخصم": "",
  "المبلغ": "",
  "{editingId ? 'تعديل خصم' : 'إضافة خصم'}": "",
  "نوع الخصم *": "",
  "اختر نوع الخصم": "",
  "المبلغ *": "",
  "بحث شامل (برقم الموظف، الاسم، الملاحظات، المبلغ...)": "",
  "تم حذف الخصم بنجاح": "",
  "السلف الشهرية": "",
  "إدارة وعرض السلف الشهرية للموظفين حسب الأشهر المالية": "",
  "عرض السلف": "",
  "كشف السلف الشهرية للموظفين": "",
  "قيمة السلفة": "",
  "(null); const [form, setForm] = useState({ ...emptyForm }); const headers = () => ({ 'Authorization': `Bearer ${localStorage.getItem('admin_token')}`, 'Accept': 'application/json', 'Content-Type': 'application/json', }); const fetchData = async () => { try { const res = await fetch(`${API}/loans/${monthId}`, { headers: headers() }); const result = await res.json(); if (result.status) { setFinanceMonth(result.finance_month); setLoans(result.loans || []); setEmployees(result.employees || []); const fm = result.finance_month; setIsMonthOpen(fm?.is_open == 1); } else { showToast(result.message, 'error'); router.push('/admin/loans'); } } catch { showToast(t('conn_error'), 'error'); } finally { setLoading(false); } }; useEffect(() => { fetchData(); }, []); const openAddModal = () => { setEditingId(null); setForm({ ...emptyForm, finance_month_period_id: monthId }); setShowModal(true); }; const openEditModal = (a: any) => { if (a.is_archived == 1) { showToast(t('cannot_edit_archived'), 'error'); return; } if (!isMonthOpen) { showToast(t('cannot_edit_closed'), 'error'); return; } setEditingId(a.id); setForm({ finance_month_period_id: monthId, employee_code: a.employee_code, total: a.total, notes: a.notes || '', }); setShowModal(true); }; const handleSave = async () => { if (!form.employee_code || !form.total) { showToast('يرجى تعبئة جميع الحقول المطلوبة', 'error'); return; } if (!editingId) { const exists = loans.some((a: any) => String(a.employee_code) === String(form.employee_code)); if (exists) { const ok = await confirm({ title: 'تنبيه', description: 'هذا الموظف لديه سلفة بالفعل في هذا الشهر. هل تريد الإضافة على أي حال؟', icon: 'warning' }); if (!ok) return; } } setSaving(true); try { const url = editingId ? `${API}/loans/${editingId}` : `${API}/loans`; const method = editingId ? 'PUT' : 'POST'; const res = await fetch(url, { method, headers: headers(), body: JSON.stringify(form) }); const result = await res.json(); if (result.status) { showToast(editingId ? 'تم تعديل السلفة بنجاح' : 'تم إضافة السلفة بنجاح', 'success'); setShowModal(false); fetchData(); } else { showToast(result.message, 'error'); } } catch { showToast(t('conn_error'), 'error'); } finally { setSaving(false); } }; const handleDelete = async (a: any) => { if (a.is_archived == 1) { showToast(t('cannot_delete_archived'), 'error'); return; } if (!isMonthOpen) { showToast(t('cannot_delete_closed'), 'error'); return; } const ok = await confirm({ title: t('confirm_delete'), description: 'هل أنت متأكد من حذف السلفة الشهرية؟', icon: 'danger' }); if (!ok) return; try { const res = await fetch(`${API}/loans/${a.id}`, { method: 'DELETE', headers: headers() }); const result = await res.json(); if (result.status) { showToast('تم حذف السلفة بنجاح', 'success'); fetchData(); } else showToast(result.message, 'error'); } catch { showToast(t('conn_error'), 'error'); } }; const filteredLoans = loans.filter((a: any) => { if (!searchQuery) return true; const query = searchQuery.toLowerCase(); const empName = a.employee?.emp_name?.toLowerCase() || ''; const empCode = a.employee_code?.toString() || ''; const notes = a.notes?.toLowerCase() || ''; const total = a.total?.toString() || ''; return empName.includes(query) || empCode.includes(query) || notes.includes(query) || total.includes(query); }); const totalAmount = filteredLoans.reduce((s: number, a: any) => s + parseFloat(a.total || 0), 0); if (loading) return": "",
  "إضافة سلفة": "",
  "{editingId ? 'تعديل سلفة' : 'إضافة سلفة'}": "",
  "قيمة السلفة *": "",
  "تم حذف السلفة بنجاح": "",
  "(null); const [loadingInst, setLoadingInst] = useState(false); const headers = () => ({ 'Authorization': `Bearer ${localStorage.getItem('admin_token')}`, 'Accept': 'application/json', 'Content-Type': 'application/json', }); const fetchData = async () => { try { const res = await fetch(`${API}/permanent-loans`, { headers: headers() }); const result = await res.json(); if (result.status) { setLoans(result.data || []); setEmployees(result.employees || []); } else { showToast(result.message, 'error'); } } catch { showToast(t('conn_error'), 'error'); } finally { setLoading(false); } }; useEffect(() => { fetchData(); }, []); const openAddModal = () => { setEditingId(null); setForm({ ...emptyForm }); setShowModal(true); }; const openEditModal = (a: any) => { if (a.is_archived == 1) { showToast('لا يمكن تعديل سلفة مؤرشفة', 'error'); return; } if (a.is_dismissal == 1) { showToast('لا يمكن تعديل سلفة تم صرفها', 'error'); return; } setEditingId(a.id); setForm({ employee_code: a.employee_code, emp_sal: a.emp_sal, total: a.total, months_number: a.months_number, monthly_installment_value: a.monthly_installment_value, year_and_month_start_date: a.year_and_month_start_date, notes: a.notes || '', }); setShowModal(true); }; const handleSave = async () => { if (!form.employee_code || !form.total || !form.months_number || !form.monthly_installment_value || !form.year_and_month_start_date) { showToast('يرجى تعبئة جميع الحقول المطلوبة', 'error'); return; } if (!editingId) { const exists = loans.some((a: any) => String(a.employee_code) === String(form.employee_code) && a.is_dismissal == 0); if (exists) { const ok = await confirm({ title: 'تنبيه', description: 'هذا الموظف لديه سلفة مستدامة أخرى قيد الانتظار لم يتم صرفها. هل تريد الإضافة على أي حال؟', icon: 'warning' }); if (!ok) return; } } setSaving(true); try { const url = editingId ? `${API}/permanent-loans/${editingId}` : `${API}/permanent-loans`; const method = editingId ? 'PUT' : 'POST'; const res = await fetch(url, { method, headers: headers(), body: JSON.stringify(form) }); const result = await res.json(); if (result.status) { showToast(result.message, 'success'); setShowModal(false); fetchData(); } else { showToast(result.message, 'error'); } } catch { showToast(t('conn_error'), 'error'); } finally { setSaving(false); } }; const handleDelete = async (a: any) => { if (a.is_archived == 1) { showToast('لا يمكن حذف سلفة مؤرشفة', 'error'); return; } if (a.is_dismissal == 1) { showToast('لا يمكن حذف سلفة تم صرفها', 'error'); return; } const ok = await confirm({ title: t('confirm_delete'), description: 'هل أنت متأكد من حذف السلفة المستدامة؟ سيتم حذف كافة الأقساط المرتبطة بها.', icon: 'danger' }); if (!ok) return; try { const res = await fetch(`${API}/permanent-loans/${a.id}`, { method: 'DELETE', headers: headers() }); const result = await res.json(); if (result.status) { showToast('تم حذف السلفة بنجاح', 'success'); fetchData(); } else showToast(result.message, 'error'); } catch { showToast(t('conn_error'), 'error'); } }; const handleDismiss = async (a: any) => { if (a.is_archived == 1) { showToast('تم أرشفة هذه السلفة', 'error'); return; } if (a.is_dismissal == 1) { showToast('تم صرف هذه السلفة مسبقاً', 'error'); return; } const ok = await confirm({ title: 'اعتماد الصرف', description: 'هل أنت متأكد من صرف هذه السلفة؟ لا يمكن التراجع أو التعديل بعد الصرف وسيبدأ الخصم من الرواتب.', icon: 'warning' }); if (!ok) return; try { const res = await fetch(`${API}/permanent-loans/${a.id}/dismiss`, { method: 'POST', headers: headers() }); const result = await res.json(); if (result.status) { showToast('تم صرف السلفة واعتماد الأقساط بنجاح', 'success'); fetchData(); } else showToast(result.message, 'error'); } catch { showToast(t('conn_error'), 'error'); } }; const openInstallments = async (a: any) => { setActiveLoan(a); setShowInstModal(true); setLoadingInst(true); try { const res = await fetch(`${API}/permanent-loans/${a.id}/installments`, { headers: headers() }); const result = await res.json(); if (result.status) { setInstallments(result.installments); } else { showToast(result.message, 'error'); } } catch { showToast(t('conn_error'), 'error'); } finally { setLoadingInst(false); } }; const handleEmployeeChange = (code: string) => { const emp = employees.find((e: any) => e.employee_code == code); setForm(f => ({ ...f, employee_code: code, emp_sal: emp?.emp_sal || '' })); }; const handleTotalChange = (total: string) => { const t = parseFloat(total) || 0; const m = parseInt(form.months_number) || 0; const install = m > 0 ? (t / m).toFixed(2) : ''; setForm(f => ({ ...f, total, monthly_installment_value: install })); }; const handleMonthsChange = (months: string) => { const m = parseInt(months) || 0; const t = parseFloat(form.total) || 0; const install = m > 0 ? (t / m).toFixed(2) : ''; setForm(f => ({ ...f, months_number: months, monthly_installment_value: install })); }; const filteredLoans = loans.filter((a: any) => { if (!searchQuery) return true; const query = searchQuery.toLowerCase(); const empName = a.employee?.emp_name?.toLowerCase() || ''; const empCode = a.employee_code?.toString() || ''; const notes = a.notes?.toLowerCase() || ''; const total = a.total?.toString() || ''; const isDismissal = a.is_dismissal == 1 ? 'تم الصرف' : 'انتظار'; const isArchived = a.is_archived == 1 ? 'مؤرشف' : 'مفتوح'; return empName.includes(query) || empCode.includes(query) || notes.includes(query) || total.includes(query) || isDismissal.includes(query) || isArchived.includes(query); }); const totalAmount = filteredLoans.reduce((s: number, a: any) => s + parseFloat(a.total || 0), 0); if (loading) return": "",
  "السلف المستدامة": "",
  "إدارة سلف الموظفين الطويلة وتوزيع الأقساط": "",
  "setSearchQuery(e.target.value)} placeholder=\"بحث شامل (برقم الموظف، الاسم، الحالة، الملاحظات، المبلغ...)\" className=\"w-full pr-12 pl-4 py-3 bg-slate-50 border border-slate-200 rounded-2xl focus:ring-2 focus:ring-indigo-500/20 outline-none text-slate-700 font-bold transition-all placeholder:text-slate-400 placeholder:font-normal\" />": "",
  "عدد الأشهر": "",
  "القسط الشهري": "",
  "حالة الصرف": "",
  "هل انتهت؟": "",
  "بدء الخصم: {a.year_and_month_start}": "",
  "تم الصرف": "",
  "بانتظار الصرف": "",
  "مؤرشفة (منتهية)": "",
  "مستمرة": "",
  "صرف": "",
  "الأقساط": "",
  "openEditModal(a)} className={`p-2 rounded-lg transition-colors ${canAct ? 'text-amber-500 hover:bg-amber-50' : 'text-slate-300 cursor-not-allowed'}`} title={!canAct ? 'لا يمكن التعديل' : t('edit')} >": "",
  "handleDelete(a)} className={`p-2 rounded-lg transition-colors ${canAct ? 'text-rose-500 hover:bg-rose-50' : 'text-slate-300 cursor-not-allowed'}`} title={!canAct ? 'لا يمكن الحذف' : t('delete')} >": "",
  "لا يوجد سلف مستدامة": "",
  "{editingId ? 'تعديل سلفة مستدامة' : 'إضافة سلفة مستدامة'}": "",
  "إجمالي السلفة *": "",
  "handleTotalChange(e.target.value)} className=\"w-full px-4 py-2.5 bg-slate-50 border border-slate-200 rounded-xl outline-none text-slate-700 font-bold focus:ring-2 focus:ring-indigo-500/20\" placeholder=\"مثال: 5000\" />": "",
  "عدد أشهر السداد *": "",
  "handleMonthsChange(e.target.value)} className=\"w-full px-4 py-2.5 bg-slate-50 border border-slate-200 rounded-xl outline-none text-slate-700 font-bold focus:ring-2 focus:ring-indigo-500/20\" placeholder=\"مثال: 5\" />": "",
  "قيمة القسط الشهري *": "",
  "تاريخ بدء الخصم *": "",
  "مثال: 2024-06-01 ليكون الخصم من شهر 6": "",
  "أقساط السلفة": "",
  "للموظف: ({activeLoan.employee_code}) - إجمالي: {activeLoan.total}": "",
  "جاري تحميل الأقساط...": "",
  "شهر الاستحقاق": "",
  "قيمة القسط": "",
  "حالة الدفع": "",
  "حالة الأرشفة": "",
  "{installments.map((inst: any, idx: number) => { const isPaid = inst.status == 1; // 1: مدفوع return (": "",
  "تم الخصم والدفع": "",
  "بانتظار الدفع": "",
  "بحث شامل (برقم الموظف، الاسم، الحالة، الملاحظات، المبلغ...)": "",
  "مثال: 5000": "",
  "مثال: 5": "",
  "يحسب تلقائياً": "",
  "لا يمكن تعديل سلفة مؤرشفة": "",
  "لا يمكن تعديل سلفة تم صرفها": "",
  "لا يمكن حذف سلفة مؤرشفة": "",
  "لا يمكن حذف سلفة تم صرفها": "",
  "تم أرشفة هذه السلفة": "",
  "تم صرف هذه السلفة مسبقاً": "",
  "تم صرف السلفة واعتماد الأقساط بنجاح": "",
  "كشف السلف المستدامة للموظفين": "",
  "القيمة الإجمالية": "",
  "المدفوع": "",
  "المتبقي": "",
  "الحالة": "",
  "{isDismissed ? 'منتهية' : 'مستمرة'}": "",
  "لا توجد سلف مستدامة": "",
  "المكافآت المالية": "",
  "إدارة السجلات الخاصة بالمكافآت المالية مقسمة حسب الأشهر المالية": "",
  "= { 1: 'مكافأة مالية', 2: 'مكافأة أيام', }; export default function RewardsPrintPage() { const { t } = useLanguage(); const params = useParams(); const monthId = params?.id as string; const [loading, setLoading] = useState(true); const [financeMonth, setFinanceMonth] = useState": "",
  "كشف مكافآت الموظفين": "",
  "نوع المكافأة": "",
  "القيمة/الأيام": "",
  "الإجمالي المالي": "",
  "{REWARD_TYPES[item.reward_type] || 'غير محدد'}": "",
  "{item.value} {item.reward_type == 2 ? 'يوم' : t('currency')}": "",
  "(null); const [form, setForm] = useState({ ...emptyForm }); const headers = () => ({ 'Authorization': `Bearer ${localStorage.getItem('admin_token')}`, 'Accept': 'application/json', 'Content-Type': 'application/json', }); const fetchData = async () => { try { const res = await fetch(`${API}/rewards/${monthId}`, { headers: headers() }); const result = await res.json(); if (result.status) { setRewards(result.data || []); setEmployees(result.employees || []); setTypes(result.types || []); setFinanceMonth(result.finance_month); setIsMonthOpen(result.finance_month?.is_open == 1); } else { showToast(result.message, 'error'); } } catch { showToast(t('conn_error'), 'error'); } finally { setLoading(false); } }; useEffect(() => { fetchData(); }, []); const openAddModal = () => { setEditingId(null); setForm({ ...emptyForm, finance_month_period_id: monthId }); setShowModal(true); }; const openEditModal = (a: any) => { if (a.is_archived == 1) { showToast(t('cannot_edit_archived'), 'error'); return; } if (!isMonthOpen) { showToast(t('cannot_edit_closed'), 'error'); return; } setEditingId(a.id); setForm({ finance_month_period_id: monthId, employee_code: a.employee_code, additions_type_id: a.additions_type_id, total: a.total, notes: a.notes || '', }); setShowModal(true); }; const handleEmployeeChange = (code: string) => { setForm(f => ({ ...f, employee_code: code })); }; const handleSave = async () => { if (!form.employee_code || !form.additions_type_id || !form.total) { showToast('يرجى تعبئة جميع الحقول المطلوبة', 'error'); return; } if (!editingId) { const exists = rewards.some((a: any) => String(a.employee_code) === String(form.employee_code) && String(a.additions_type_id) === String(form.additions_type_id)); if (exists) { const ok = await confirm({ title: 'تنبيه', description: 'هذا الموظف لديه مكافأة من نفس النوع في هذا الشهر. هل تريد الإضافة على أي حال؟', icon: 'warning' }); if (!ok) return; } } setSaving(true); try { const url = editingId ? `${API}/rewards/${editingId}` : `${API}/rewards`; const method = editingId ? 'PUT' : 'POST'; const res = await fetch(url, { method, headers: headers(), body: JSON.stringify(form), }); const result = await res.json(); if (result.status) { showToast('تم حفظ المكافأة بنجاح', 'success'); setShowModal(false); fetchData(); } else { showToast(result.message, 'error'); } } catch { showToast(t('conn_error'), 'error'); } finally { setSaving(false); } }; const handleDelete = async (a: any) => { if (a.is_archived == 1) { showToast(t('cannot_delete_archived'), 'error'); return; } if (!isMonthOpen) { showToast(t('cannot_delete_closed'), 'error'); return; } const ok = await confirm({ title: t('confirm_delete'), description: 'هل أنت متأكد من حذف المكافأة؟', icon: 'danger' }); if (!ok) return; try { const res = await fetch(`${API}/rewards/${a.id}`, { method: 'DELETE', headers: headers() }); const result = await res.json(); if (result.status) { showToast('تم حذف المكافأة بنجاح', 'success'); fetchData(); } else showToast(result.message, 'error'); } catch { showToast(t('conn_error'), 'error'); } }; const filteredRewards = rewards.filter((a: any) => { if (!searchQuery) return true; const query = searchQuery.toLowerCase(); const empName = a.employee?.emp_name?.toLowerCase() || ''; const empCode = a.employee_code?.toString() || ''; const notes = a.notes?.toLowerCase() || ''; const total = a.total?.toString() || ''; const typeObj = types.find((t: any) => t.id === a.additions_type_id); const typeName = typeObj?.name?.toLowerCase() || ''; return empName.includes(query) || empCode.includes(query) || notes.includes(query) || total.includes(query) || typeName.includes(query); }); const total = filteredRewards.reduce((sum: number, a: any) => sum + parseFloat(a.total || 0), 0); if (loading) return": "",
  "إضافة مكافأة": "",
  "عدد المكافآت": "",
  "setSearchQuery(e.target.value)} placeholder=\"بحث شامل (برقم الموظف، الاسم، نوع المكافأة، الملاحظات، المبلغ...)\" className=\"w-full pr-12 pl-4 py-3 bg-slate-50 border border-slate-200 rounded-2xl focus:ring-2 focus:ring-emerald-500/20 outline-none text-slate-700 font-bold transition-all placeholder:text-slate-400 placeholder:font-normal\" />": "",
  "{filteredRewards.map((a: any) => { const isArchived = a.is_archived == 1; const canAct = isMonthOpen && !isArchived; const emp = employees.find((e: any) => String(e.employee_code) === String(a.employee_code)); const empName = a.employee?.emp_name || emp?.emp_name || a.employee_code; const typeName = types.find((t: any) => t.id == a.additions_type_id)?.name || 'غير محدد'; return (": "",
  "لا يوجد مكافآت": "",
  "{editingId ? 'تعديل المكافأة' : 'إضافة مكافأة جديدة'}": "",
  "نوع المكافأة *": "",
  "اختر نوع المكافأة": "",
  "بحث شامل (برقم الموظف، الاسم، نوع المكافأة، الملاحظات، المبلغ...)": "",
  "تم حفظ المكافأة بنجاح": "",
  "تم حذف المكافأة بنجاح": "",
  "= { 1: 'يناير', 2: 'فبراير', 3: 'مارس', 4: 'أبريل', 5: 'مايو', 6: 'يونيو', 7: 'يوليو', 8: 'أغسطس', 9: 'سبتمبر', 10: 'أكتوبر', 11: 'نوفمبر', 12: 'ديسمبر' }; return months[item.month_id] || item.month_id; }; const getRowAction = (item: any) => { if (item.is_open == 2) return 'archived'; if (item.is_open == 1) return 'view'; return 'pending'; // Sanctions shouldn't be \"opened\" from here, just viewed or pending. }; if (loading) return": "",
  "= { 1: 'جزاء أيام', 2: 'جزاء مبلغ', 3: 'جزاء بالبصمة', }; export default function SanctionsPrintPage() { const { t } = useLanguage(); const params = useParams(); const monthId = params?.id as string; const [loading, setLoading] = useState(true); const [financeMonth, setFinanceMonth] = useState": "",
  "كشف جزاءات الموظفين": "",
  "نوع الجزاء": "",
  "أيام": "",
  "قيمة الجزاء": "",
  "{SANCTION_TYPES[item.sanctions_type] || 'غير محدد'}": "",
  "= { 1: { label: 'جزاء أيام', color: 'bg-rose-100 text-rose-700' }, 2: { label: 'جزاء بصمة', color: 'bg-orange-100 text-orange-700' }, 3: { label: 'جزاء تحقيق', color: 'bg-purple-100 text-purple-700' }, }; const emptyForm = { finance_month_period_id: '', employee_code: '', day_price: '', sanctions_type: '', value: '', notes: '', }; export default function SanctionsDetailPage() { const { t } = useLanguage(); const { showToast } = useToast(); const { confirm } = useConfirm(); const params = useParams(); const router = useRouter(); const monthId = params?.id as string; const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [financeMonth, setFinanceMonth] = useState": "",
  "(null); const [form, setForm] = useState({ ...emptyForm }); const headers = () => ({ 'Authorization': `Bearer ${localStorage.getItem('admin_token')}`, 'Accept': 'application/json', 'Content-Type': 'application/json', }); const fetchData = async () => { try { const res = await fetch(`${API}/sanctions/${monthId}`, { headers: headers() }); const result = await res.json(); if (result.status) { setFinanceMonth(result.finance_month); setSanctions(result.sanctions || []); setEmployees(result.employees || []); // Month open check: finance year is open AND this month is_open const fm = result.finance_month; setIsMonthOpen(fm?.is_open == 1); } else { showToast(result.message, 'error'); router.push('/admin/sanctions'); } } catch { showToast(t('conn_error'), 'error'); } finally { setLoading(false); } }; useEffect(() => { fetchData(); }, []); const openAddModal = () => { setEditingId(null); setForm({ ...emptyForm, finance_month_period_id: monthId }); setShowModal(true); }; const openEditModal = (s: any) => { if (s.is_archived == 1) { showToast(t('cannot_edit_archived'), 'error'); return; } if (!isMonthOpen) { showToast(t('cannot_edit_closed'), 'error'); return; } setEditingId(s.id); setForm({ finance_month_period_id: monthId, employee_code: s.employee_code, day_price: s.day_price, sanctions_type: s.sanctions_type, value: s.value, notes: s.notes || '', }); setShowModal(true); }; const handleEmployeeChange = (code: string) => { const emp = employees.find((e: any) => e.employee?.employee_code == code || e.employee_code == code); const dayPrice = emp?.employee?.day_price || emp?.day_price || ''; setForm(f => ({ ...f, employee_code: code, day_price: dayPrice })); }; const handleSave = async () => { if (!form.employee_code || !form.sanctions_type || !form.value || !form.day_price) { showToast('يرجى تعبئة جميع الحقول المطلوبة', 'error'); return; } if (!editingId) { const exists = sanctions.some((a: any) => String(a.employee_code) === String(form.employee_code)); if (exists) { const ok = await confirm({ title: 'تنبيه', description: 'هذا الموظف لديه سجل جزاء بالفعل في هذا الشهر. هل تريد الإضافة على أي حال؟', icon: 'warning' }); if (!ok) return; } } setSaving(true); try { const url = editingId ? `${API}/sanctions/${editingId}` : `${API}/sanctions`; const method = editingId ? 'PUT' : 'POST'; const res = await fetch(url, { method, headers: headers(), body: JSON.stringify(form), }); const result = await res.json(); if (result.status) { showToast(editingId ? t('sanction_updated') : t('sanction_added'), 'success'); setShowModal(false); fetchData(); } else { showToast(result.message, 'error'); } } catch { showToast(t('conn_error'), 'error'); } finally { setSaving(false); } }; const handleDelete = async (s: any) => { if (s.is_archived == 1) { showToast(t('cannot_delete_archived'), 'error'); return; } if (!isMonthOpen) { showToast(t('cannot_delete_closed'), 'error'); return; } const ok = await confirm({ title: t('confirm_delete'), description: 'هل أنت متأكد من حذف هذا الجزاء؟', icon: 'danger' }); if (!ok) return; try { const res = await fetch(`${API}/sanctions/${s.id}`, { method: 'DELETE', headers: headers() }); const result = await res.json(); if (result.status) { showToast(t('sanction_deleted'), 'success'); fetchData(); } else showToast(result.message, 'error'); } catch { showToast(t('conn_error'), 'error'); } }; const filteredSanctions = sanctions.filter((s: any) => { if (!searchQuery) return true; const query = searchQuery.toLowerCase(); const empName = s.employee?.emp_name?.toLowerCase() || ''; const empCode = s.employee_code?.toString() || ''; const typeLabel = SANCTION_TYPES[s.sanctions_type]?.label?.toLowerCase() || ''; const notes = s.notes?.toLowerCase() || ''; const value = s.value?.toString() || ''; const total = s.total?.toString() || ''; return empName.includes(query) || empCode.includes(query) || typeLabel.includes(query) || notes.includes(query) || value.includes(query) || total.includes(query); }); const total = filteredSanctions.reduce((sum: number, s: any) => sum + parseFloat(s.total || 0), 0); const days = filteredSanctions.reduce((sum: number, s: any) => sum + parseFloat(s.value || 0), 0); if (loading) return": "",
  "setSearchQuery(e.target.value)} placeholder=\"بحث شامل (برقم الموظف، الاسم، نوع الجزاء، الملاحظات، المبلغ...)\" className=\"w-full pr-12 pl-4 py-3 bg-slate-50 border border-slate-200 rounded-2xl focus:ring-2 focus:ring-emerald-500/20 outline-none text-slate-700 font-bold transition-all placeholder:text-slate-400 placeholder:font-normal\" />": "",
  "بحث شامل (برقم الموظف، الاسم، نوع الجزاء، الملاحظات، المبلغ...)": "",
  "جزاء أيام": "",
  "جزاء بصمة": "",
  "جزاء تحقيق": "",
  "= { 1: 'يناير', 2: 'فبراير', 3: 'مارس', 4: 'أبريل', 5: 'مايو', 6: 'يونيو', 7: 'يوليو', 8: 'أغسطس', 9: 'سبتمبر', 10: 'أكتوبر', 11: 'نوفمبر', 12: 'ديسمبر' }; export default function EmployeeSalaryDetailsPage() { const { t } = useLanguage(); const { showToast } = useToast(); const { confirm } = useConfirm(); const params = useParams(); const router = useRouter(); const recordId = params?.id as string; const [loading, setLoading] = useState(true); const [data, setData] = useState": "",
  "(null); const [isMonthOpen, setIsMonthOpen] = useState(false); const [actionLoading, setActionLoading] = useState(false); const headers = () => ({ 'Authorization': `Bearer ${localStorage.getItem('admin_token')}`, 'Accept': 'application/json', 'Content-Type': 'application/json', }); const fetchData = async () => { try { const res = await fetch(`${API}/employee-salaries/${recordId}/details`, { headers: headers() }); const result = await res.json(); if (result.status) { setData(result.data); setFinanceMonth(result.finance_month); setIsMonthOpen(result.finance_month?.is_open == 1); } else { showToast(result.message || 'فشل في تحميل تفاصيل الراتب', 'error'); router.back(); } } catch { showToast('حدث خطأ في الاتصال بالخادم', 'error'); } finally { setLoading(false); } }; useEffect(() => { if (recordId) { fetchData(); } }, [recordId]); const handleStopSalary = async () => { const ok = await confirm({ title: 'تأكيد إيقاف الراتب', description: 'هل أنت متأكد من إيقاف راتب هذا الموظف للشهر الحالي؟', icon: 'warning' }); if (!ok) return; setActionLoading(true); try { const res = await fetch(`${API}/employee-salaries/${recordId}/stop`, { method: 'POST', headers: headers() }); const result = await res.json(); if (result.status) { showToast(result.message, 'success'); fetchData(); } else { showToast(result.message, 'error'); } } catch { showToast('حدث خطأ في الاتصال بالخادم', 'error'); } finally { setActionLoading(false); } }; const handleResumeSalary = async () => { const ok = await confirm({ title: 'تأكيد تفعيل الراتب', description: 'هل أنت متأكد من إعادة تفعيل راتب هذا الموظف؟', icon: 'warning' }); if (!ok) return; setActionLoading(true); try { const res = await fetch(`${API}/employee-salaries/${recordId}/resume`, { method: 'POST', headers: headers() }); const result = await res.json(); if (result.status) { showToast(result.message, 'success'); fetchData(); } else { showToast(result.message, 'error'); } } catch { showToast('حدث خطأ في الاتصال بالخادم', 'error'); } finally { setActionLoading(false); } }; const handleArchiveSalary = async () => { const ok = await confirm({ title: 'تأكيد أرشفة الراتب', description: 'هل أنت متأكد من أرشفة راتب هذا الموظف؟ سيتم إغلاق كافة الحركات والجزاءات المرتبطة به ولا يمكن التعديل عليها لاحقاً.', icon: 'warning' }); if (!ok) return; setActionLoading(true); try { const res = await fetch(`${API}/employee-salaries/${recordId}/archive`, { method: 'POST', headers: headers() }); const result = await res.json(); if (result.status) { showToast(result.message, 'success'); fetchData(); } else { showToast(result.message, 'error'); } } catch { showToast('حدث خطأ في الاتصال بالخادم', 'error'); } finally { setActionLoading(false); } }; const handleDeleteSalary = async () => { const ok = await confirm({ title: 'تأكيد حذف الراتب', description: 'هل أنت متأكد من حذف سجل راتب الموظف بالكامل لهذا الشهر؟ لا يمكن التراجع عن هذا الإجراء.', icon: 'danger' }); if (!ok) return; setActionLoading(true); try { const res = await fetch(`${API}/employee-salaries/${recordId}`, { method: 'DELETE', headers: headers() }); const result = await res.json(); if (result.status) { showToast(result.message, 'success'); router.push(`/admin/employee-salaries/${data.finance_month_id}`); } else { showToast(result.message, 'error'); } } catch { showToast('حدث خطأ في الاتصال بالخادم', 'error'); } finally { setActionLoading(false); } }; const handlePrint = () => { window.open(`/admin/employee-salaries/print/${recordId}`, '_blank'); }; if (loading) return": "",
  "تفاصيل راتب الموظف": "",
  "الشهر المالي: {MONTHS_AR[financeMonth.month_id]} لسنة {financeMonth.finance_yr}": "",
  "طباعة كشف الراتب": "",
  "حالة الراتب الحالية:": "",
  "مؤرشف ومغلق بالكامل": "",
  "موقوف مؤقتاً": "",
  "نشط ومفعّل": "",
  "إيقاف مؤقت": "",
  "أرشفة الراتب": "",
  "تفعيل الراتب": "",
  "حذف السجل": "",
  "كشف راتب موظف تفصيلي": "",
  "عن الشهر المالي: {MONTHS_AR[financeMonth.month_id]} لسنة {financeMonth.finance_yr} ({financeMonth.start_date_m} إلى {financeMonth.end_date_m})": "",
  "الموظف": "",
  "كود الموظف: {data.employee_code}": "",
  "الوظيفة": "",
  "{data.emp_job_id ? `مسمى وظيفي رقم ${data.emp_job_id}` : 'غير محدد'}": "",
  "القسم: {data.emp_departments_id ? `قسم رقم ${data.emp_departments_id}` : 'عام'}": "",
  "سعر اليوم / نوع الصرف": "",
  "{parseFloat(data.day_price || 0).toFixed(2)} {t('currency')} / يوم": "",
  "طريقة الصرف: {data.sal_cash_or_visa == 1 ? 'كاش (نقدي)' : 'فيزا (بنكي)'}": "",
  "الرصيد المرحل من الشهر السابق:": "",
  "أولاً: الاستحقاقات": "",
  "الراتب الأساسي": "",
  "الحافز الثابت": "",
  "المكافأة المالية": "",
  "البدلات الثابتة": "",
  "أيام إضافية": "",
  "عدد الأيام: ({data.additional_days_counter})": "",
  "إجمالي الاستحقاقات": "",
  "ثانياً: الاستقطاعات": "",
  "التأمين الاجتماعي": "",
  "التأمين الطبي": "",
  "عدد الأيام: ({data.absence_days_counter})": "",
  "أيام الجزاءات": "",
  "عدد الأيام: ({data.sanctions_days_counter})": "",
  "سلف شهرية": "",
  "سلف مستديمة": "",
  "فواتير الهواتف": "",
  "إجمالي الاستقطاعات": "",
  "ثالثاً: صافي الراتب المستحق": "",
  "مبلغ مستحق": "",
  "لصالح": "",
  "على": "",
  "الموظف (مديونية)": "",
  "صافي الراتب متزن تماماً": "",
  "الصافي النهائي للتسليم": "",
  "تمت أرشفة وإغلاق هذا السجل بتاريخ {data.archived_date ? new Date(data.archived_date).toLocaleString('ar-YE') : 'محدد مسبقاً'}": "",
  "توقيع المسؤول": "",
  "توقيع الموظف المستلم": "",
  "حدث خطأ في الاتصال بالخادم": "",
  "([]); const [loading, setLoading] = useState(true); const [filterYear, setFilterYear] = useState('all'); const token = () => localStorage.getItem('admin_token'); const headers = { 'Authorization': `Bearer ${token()}`, 'Accept': 'application/json' }; const fetchData = async () => { setLoading(true); try { const params = filterYear !== 'all' ? `?finance_yr=${filterYear}` : ''; const res = await fetch(`${API}/employee-salaries/months${params}`, { headers }); const result = await res.json(); if (result.status) { setData(result.data || []); setFinanceYears(result.finance_years || []); } } catch { showToast('حدث خطأ في الاتصال', 'error'); } finally { setLoading(false); } }; useEffect(() => { fetchData(); }, [filterYear]); const getRowAction = (item: any) => { const yearIsOpen = item.currentYear?.is_open == 1; const noOtherMonthOpen = item.counterOpenMonth === 0; const noPreviousPending = item.counterPreviousMonthWaitingOpen === 0; if (item.is_open == 2) return 'archived'; if (item.is_open == 1) return 'view'; if (item.is_open == 0 && yearIsOpen && noOtherMonthOpen && noPreviousPending) return 'open'; return 'pending'; }; const getMonthLabel = (item: any) => { const monthsAr: Record": "",
  "= { 1: 'يناير', 2: 'فبراير', 3: 'مارس', 4: 'أبريل', 5: 'مايو', 6: 'يونيو', 7: 'يوليو', 8: 'أغسطس', 9: 'سبتمبر', 10: 'أكتوبر', 11: 'نوفمبر', 12: 'ديسمبر' }; return monthsAr[item.month_id] || item.month_id; }; if (loading && data.length === 0) return": "",
  "رواتب الموظفين مفصلة": "",
  "عرض رواتب الموظفين الشهرية المفصلة": "",
  "جميع السنوات": "",
  "عرض الرواتب": "",
  "لا توجد سجلات رواتب": "",
  "= { 1: 'يناير', 2: 'فبراير', 3: 'مارس', 4: 'أبريل', 5: 'مايو', 6: 'يونيو', 7: 'يوليو', 8: 'أغسطس', 9: 'سبتمبر', 10: 'أكتوبر', 11: 'نوفمبر', 12: 'ديسمبر' }; export default function PrintSalarySlipPage() { const params = useParams(); const router = useRouter(); const recordId = params?.id as string; const [loading, setLoading] = useState(true); const [data, setData] = useState": "",
  "جاري تجهيز المستند للطباعة...": "",
  "لم يتم العثور على بيانات هذا الراتب": "",
  "طباعة": "",
  "window.close()} className=\"bg-white text-slate-700 px-6 py-2 rounded-lg font-bold border border-slate-300 hover:bg-slate-50 transition-colors\"> إغلاق": "",
  "نظام إدارة الموارد البشرية": "",
  "كشف تفاصيل راتب الموظف": "",
  "الشهر المالي": "",
  "تاريخ الطباعة: {new Date().toLocaleDateString('ar-SA')}": "",
  "رقم الموظف": "",
  "الاسم الكامل": "",
  "رقم الهوية": "",
  "طريقة الصرف": "",
  "{data.sal_cash_or_visa == 1 ? 'كاش (نقدي)' : 'فيزا (بنكي)'}": "",
  "الإدارة": "",
  "{data.emp_departments_id ? `إدارة كود ${data.emp_departments_id}` : '-'}": "",
  "المسمى الوظيفي": "",
  "{data.emp_job_id ? `مسمى وظيفي كود ${data.emp_job_id}` : '-'}": "",
  "الاستحقاقات": "",
  "بدلات ثابتة": "",
  "بدلات متغيرة": "",
  "إضافي أيام": "",
  "الاستقطاعات": "",
  "تأمين اجتماعي": "",
  "تأمين طبي": "",
  "خصم الغيابات": "",
  "خصم الجزاءات": "",
  "الصافي النهائي للاستلام (Net Salary)": "",
  "المبلغ النهائي بعد كافة الاستحقاقات والاستقطاعات": "",
  "ريال": "",
  "توقيع الموظف": "",
  "المدير المالي / المحاسب": "",
  "مدير الموارد البشرية": "",
  "= { 1: 'يناير', 2: 'فبراير', 3: 'مارس', 4: 'أبريل', 5: 'مايو', 6: 'يونيو', 7: 'يوليو', 8: 'أغسطس', 9: 'سبتمبر', 10: 'أكتوبر', 11: 'نوفمبر', 12: 'ديسمبر' }; export default function SalaryRecordsDetailPage() { const { showToast } = useToast(); const { confirm } = useConfirm(); const params = useParams(); const router = useRouter(); const monthId = params?.id as string; const [loading, setLoading] = useState(true); const [financeMonth, setFinanceMonth] = useState": "",
  "([]); const [nothavesal, setNothavesal] = useState(0); const [isMonthOpen, setIsMonthOpen] = useState(false); const [saving, setSaving] = useState(false); // Add salary modal const [showAddModal, setShowAddModal] = useState(false); const [selectedEmployee, setSelectedEmployee] = useState(''); // Filters const [searchQuery, setSearchQuery] = useState(''); const headers = () => ({ 'Authorization': `Bearer ${localStorage.getItem('admin_token')}`, 'Accept': 'application/json', 'Content-Type': 'application/json', }); const fetchData = async () => { try { const res = await fetch(`${API}/employee-salaries/${monthId}`, { headers: headers() }); const result = await res.json(); if (result.status) { setFinanceMonth(result.finance_month); setData(result.data || []); setAvailableEmployees(result.available_employees || []); setNothavesal(result.nothavesal || 0); setIsMonthOpen(result.finance_month?.is_open == 1); } else { showToast(result.message, 'error'); router.push('/admin/employee-salaries'); } } catch { showToast('خطأ في الاتصال بالخادم', 'error'); } finally { setLoading(false); } }; useEffect(() => { fetchData(); }, []); const handleAddSalary = async () => { if (!selectedEmployee) { showToast('يرجى اختيار الموظف', 'error'); return; } setSaving(true); try { const res = await fetch(`${API}/employee-salaries`, { method: 'POST', headers: headers(), body: JSON.stringify({ finance_month_id: monthId, employee_code: selectedEmployee }), }); const result = await res.json(); if (result.status) { showToast('تم إضافة راتب الموظف بنجاح', 'success'); setShowAddModal(false); setSelectedEmployee(''); fetchData(); } else { showToast(result.message, 'error'); } } catch { showToast('حدث خطأ في الاتصال', 'error'); } finally { setSaving(false); } }; const handleDelete = async (id: number) => { const ok = await confirm({ title: 'تأكيد الحذف', description: 'هل أنت متأكد من حذف راتب هذا الموظف؟', icon: 'danger' }); if (!ok) return; try { const res = await fetch(`${API}/employee-salaries/${id}`, { method: 'DELETE', headers: headers() }); const result = await res.json(); if (result.status) { showToast('تم الحذف بنجاح', 'success'); fetchData(); } else showToast(result.message, 'error'); } catch { showToast('حدث خطأ في الاتصال', 'error'); } }; const handleArchive = async (id: number) => { const ok = await confirm({ title: 'تأكيد الأرشفة', description: 'هل أنت متأكد من أرشفة راتب هذا الموظف؟ ستُغلق جميع الجزاءات والخصومات والغياب المرتبطة.', icon: 'warning' }); if (!ok) return; try { const res = await fetch(`${API}/employee-salaries/${id}/archive`, { method: 'POST', headers: headers() }); const result = await res.json(); if (result.status) { showToast('تم أرشفة الراتب بنجاح', 'success'); fetchData(); } else showToast(result.message, 'error'); } catch { showToast('حدث خطأ في الاتصال', 'error'); } }; const handleStop = async (id: number, currentlyStopped: boolean) => { const action = currentlyStopped ? 'resume' : 'stop'; const msg = currentlyStopped ? 'هل تريد تفعيل هذا الراتب؟' : 'هل تريد إيقاف هذا الراتب؟'; const ok = await confirm({ title: 'تأكيد', description: msg, icon: 'warning' }); if (!ok) return; try { const res = await fetch(`${API}/employee-salaries/${id}/${action}`, { method: 'POST', headers: headers() }); const result = await res.json(); if (result.status) { showToast(result.message, 'success'); fetchData(); } else showToast(result.message, 'error'); } catch { showToast('حدث خطأ في الاتصال', 'error'); } }; const filteredData = data.filter((item: any) => { if (!searchQuery) return true; const query = searchQuery.toLowerCase(); const empName = item.employee?.emp_name?.toLowerCase() || ''; const empCode = item.employee_code?.toString() || ''; const isArchived = item.is_archived == 1 ? 'مؤرشف' : 'غير مؤرشف'; const isStoped = item.is_stoped == 1 ? 'موقوف' : 'ساري'; const finalNet = item.final_the_net?.toString() || ''; const totalBenefits = item.total_benefits?.toString() || ''; const totalDeduction = item.total_deduction?.toString() || ''; return empName.includes(query) || empCode.includes(query) || isArchived.includes(query) || isStoped.includes(query) || finalNet.includes(query) || totalBenefits.includes(query) || totalDeduction.includes(query); }); const totalBenefits = filteredData.reduce((s: number, r: any) => s + parseFloat(r.total_benefits || 0), 0); const totalDeduction = filteredData.reduce((s: number, r: any) => s + parseFloat(r.total_deduction || 0), 0); const totalNet = filteredData.reduce((s: number, r: any) => s + parseFloat(r.final_the_net || 0), 0); if (loading) return": "",
  "سجلات الرواتب": "",
  "السنة المالية: {financeMonth.finance_yr}": "",
  "{MONTHS_AR[financeMonth.month_id]} — {financeMonth.start_date_m} إلى {financeMonth.end_date_m}": "",
  "{isMonthOpen ? 'مفتوح' : 'مغلق'}": "",
  "إضافة راتب موظف": "",
  "{[ { label: 'إجمالي الموظفين', value: filteredData.length, color: 'text-violet-700', bg: 'bg-violet-50', icon:": "",
  "}, { label: 'إجمالي الاستحقاقات', value: totalBenefits.toFixed(2), color: 'text-emerald-700', bg: 'bg-emerald-50', icon:": "",
  "}, { label: 'إجمالي الخصومات', value: totalDeduction.toFixed(2), color: 'text-rose-700', bg: 'bg-rose-50', icon:": "",
  "}, { label: 'الصافي النهائي', value: totalNet.toFixed(2), color: 'text-blue-700', bg: 'bg-blue-50', icon:": "",
  "setSearchQuery(e.target.value)} placeholder=\"بحث شامل (برقم الموظف، الاسم، الحالة، الإجمالي، الصافي...)\" className=\"w-full pr-12 pl-4 py-3 bg-slate-50 border border-slate-200 rounded-2xl focus:ring-2 focus:ring-violet-500/20 outline-none text-slate-700 font-bold transition-all placeholder:text-slate-400 placeholder:font-normal\" />": "",
  "الخصومات": "",
  "الصافي": "",
  "إجراءات": "",
  "موقوف": "",
  "نشط": "",
  "handleArchive(row.id)} className=\"p-2 rounded-lg text-slate-500 hover:bg-slate-100 transition-colors\" title=\"أرشفة الراتب\">": "",
  "handleStop(row.id, isStopped)} className={`p-2 rounded-lg transition-colors ${isStopped ? 'text-emerald-500 hover:bg-emerald-50' : 'text-amber-500 hover:bg-amber-50'}`} title={isStopped ? 'تفعيل الراتب' : 'إيقاف الراتب'}> {isStopped ? (": "",
  "handleDelete(row.id)} className=\"p-2 rounded-lg text-rose-500 hover:bg-rose-50 transition-colors\" title=\"حذف\">": "",
  "لا توجد سجلات رواتب لهذا الشهر": "",
  "setShowAddModal(true)} className=\"mt-4 bg-violet-500 text-white px-5 py-2 rounded-xl font-bold hover:bg-violet-600 transition-all text-sm\"> إضافة راتب موظف": "",
  "اختر الموظف *": "",
  "-- اختر موظفاً --": "",
  "{e.emp_name} ({e.employee_code}) — راتب: {parseFloat(e.emp_sal || 0).toFixed(2)}": "",
  "يُعرض فقط الموظفون الذين لا يملكون سجلاً في هذا الشهر": "",
  "setShowAddModal(false)} className=\"px-5 py-2.5 rounded-xl border border-slate-200 text-slate-600 font-bold hover:bg-slate-50\"> إلغاء": "",
  "{saving ? 'جاري الإضافة...' : 'إضافة'}": "",
  "بحث شامل (برقم الموظف، الاسم، الحالة، الإجمالي، الصافي...)": "",
  "عرض التفاصيل": "",
  "خطأ في الاتصال بالخادم": "",
  "يرجى اختيار الموظف": "",
  "تم إضافة راتب الموظف بنجاح": "",
  "تم الحذف بنجاح": "",
  "تم أرشفة الراتب بنجاح": "",
  "إجمالي الموظفين": "",
  "إجمالي الخصومات": "",
  "الصافي النهائي": "",
  "= { 1: 'يناير', 2: 'فبراير', 3: 'مارس', 4: 'أبريل', 5: 'مايو', 6: 'يونيو', 7: 'يوليو', 8: 'أغسطس', 9: 'سبتمبر', 10: 'أكتوبر', 11: 'نوفمبر', 12: 'ديسمبر' }; export default function SalaryRecordsPage() { const { t } = useLanguage(); const { showToast } = useToast(); const [data, setData] = useState": "",
  "(null); const [pasmaDates, setPasmaDates] = useState({ start: '', end: '' }); const [loadingPasma, setLoadingPasma] = useState(false); const token = () => localStorage.getItem('admin_token'); const headers = { 'Authorization': `Bearer ${token()}`, 'Accept': 'application/json' }; const fetchData = async (currentPage = 1, year = filterYear) => { setLoading(true); try { const params = new URLSearchParams({ page: currentPage.toString() }); if (year) params.append('finance_yr', year); const res = await fetch(`${API}/salary-records?${params.toString()}`, { headers }); const result = await res.json(); if (result.status) { setData(result.data || []); setFinanceYears(result.finance_years || []); setPagination(result.pagination); if (result.active_year && !filterYear) setFilterYear(result.active_year); } } catch { showToast('حدث خطأ في الاتصال', 'error'); } finally { setLoading(false); } }; useEffect(() => { fetchData(page, filterYear); }, [page, filterYear]); const getRowAction = (item: any) => { const yearIsOpen = item.currentYear?.is_open == 1; const noOtherMonthOpen = item.counterOpenMonth === 0; const noPreviousPending = item.counterPreviousMonthWaitingOpen === 0; if (item.is_open == 2) return 'archived'; if (item.is_open == 1) return 'view'; if (item.is_open == 0 && yearIsOpen && noOtherMonthOpen && noPreviousPending) return 'open'; return 'pending'; }; const handleOpenMonthClick = async (id: number) => { setOpenMonthModal(id); setLoadingPasma(true); try { const res = await fetch(`${API}/salary-records/pasma-dates/${id}`, { headers }); const result = await res.json(); if (result.status) { setPasmaDates({ start: result.start_date_for_pasma || '', end: result.end_date_for_pasma || '' }); } } catch { showToast('خطأ في جلب تواريخ البصمة', 'error'); } finally { setLoadingPasma(false); } }; const submitOpenMonth = async () => { if (!openMonthModal) return; if (!pasmaDates.start || !pasmaDates.end) { showToast('يرجى تحديد تواريخ البصمة', 'error'); return; } try { const res = await fetch(`${API}/salary-records/open-month/${openMonthModal}`, { method: 'POST', headers: { ...headers, 'Content-Type': 'application/json' }, body: JSON.stringify({ start_date_for_pasma: pasmaDates.start, end_date_for_pasma: pasmaDates.end }) }); const result = await res.json(); if (result.status) { showToast(result.message, 'success'); setOpenMonthModal(null); fetchData(page, filterYear); } else { showToast(result.message, 'error'); } } catch { showToast('حدث خطأ أثناء فتح الشهر', 'error'); } }; const handleCloseMonth = async (id: number) => { if (!confirm('هل أنت متأكد من إغلاق وأرشفة هذا الشهر المالي بالكامل؟ لا يمكن التراجع عن هذه الخطوة.')) return; try { const res = await fetch(`${API}/salary-records/close-month/${id}`, { method: 'POST', headers }); const result = await res.json(); if (result.status) { showToast(result.message, 'success'); fetchData(page, filterYear); } else { showToast(result.message, 'error'); } } catch { showToast('حدث خطأ في الاتصال', 'error'); } }; if (loading && !data.length) return": "",
  "إدارة سجلات رواتب الموظفين الشهرية لكل شهر مالي": "",
  "handleCloseMonth(item.id)} className=\"px-3 py-1.5 bg-rose-100 text-rose-700 hover:bg-rose-200 rounded-lg transition-colors font-bold text-xs\">إغلاق": "",
  "handleOpenMonthClick(item.id)} className=\"px-3 py-1.5 bg-violet-100 text-violet-700 hover:bg-violet-200 rounded-lg transition-colors font-bold text-xs\">فتح": "",
  "لا توجد أشهر مالية مفتوحة أو مغلقة": "",
  "عرض {data.length} من أصل {pagination.total} سجل": "",
  "فتح الشهر المالي": "",
  "تاريخ بداية البصمة": "",
  "تاريخ نهاية البصمة": "",
  "تأكيد فتح الشهر": "",
  "setOpenMonthModal(null)} className=\"flex-1 bg-slate-100 text-slate-700 py-3 rounded-xl font-bold hover:bg-slate-200 transition-colors\" > إلغاء": "",
  "خطأ في جلب تواريخ البصمة": "",
  "يرجى تحديد تواريخ البصمة": "",
  "حدث خطأ أثناء فتح الشهر": "",
  "هل أنت متأكد من إغلاق وأرشفة هذا الشهر المالي بالكامل؟ لا يمكن التراجع عن هذه الخطوة.": ""
}